<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <title>Lab 11</title>
</head>

<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #2596be">
    <div class="collapse navbar-collapse d-flex justify-content-between">
        <ul class="navbar-nav mr-auto">
            <% if(user.isLogged) { %>
            <li>
                <a class="nav-link" href="/" style="color: #f3f1f1">Нүүр</a>
            </li>
            <%} %>
            <% if(user.isAdmin) { %>
            <li>
                <a class="nav-link" href="/users" style="color: #f3f1f1">Хэрэглэгчид</a>
            </li>
            <%} %>
            <% if(!user.isLogged) { %>
            <li>
                <a class="nav-link" href="/login" style="color: #f3f1f1">Нэвтрэх</a>
            </li>

            <li>
                <a class="nav-link" href="/register" style="color: #f3f1f1">Бүртгүүлэх</a>
            </li>
            <%} %>
        </ul>
        <% if(user.isOperator) { %>
        <form class="form d-flex justify-content-start" action="" method="GET">
            <input class="form-control mr-sm-2" type="search" placeholder="Хайх" aria-label="Search" id="search" name="search">
            <button class="btn btn-outline" type="submit"
                style="color: #f3f1f1; border: #f3f1f1 1px solid">Хайх</button>
        </form>
        <%} %>
        <div>
            <a class="navbar-brand d-inline-block" href="/" style="color: #f3f1f1">
                <%= user.username %>
            </a>
            <% if(user.isLogged) { %>
            <form action="/logout" method="POST" class="d-inline-block">
                <button type="submit" class="btn btn-danger">Logout</button>
            </form>
            <%} %>
        </div>

    </div>
</nav>

<body style="background-color: #f8f4f4;">
    <div style="min-height: 76vh">